<%= turbo_frame_tag "modal" do %>
  <h1 class="h3 modal-header">
    <%= t('pages.categories.add') %>
  </h1>

  <div class="modal-body">
    <%= form_for @category, url: administrators_categories_path do |f|%>
      <%= render "form", f: f %>
    <% end %>
  </div>
<% end %>
